rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura a todos
    match /{document=**} {
      allow read: if true;
      // Solo escritura a usuarios autenticados
      allow write: if request.auth != null;
    }
  }
}